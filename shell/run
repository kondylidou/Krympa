#!/bin/bash

BENCHMARK_DIR="../benchmarks"
PYTHON_SCRIPT="../python/generate_input.py"
RUST_DIR="../rust"
BENCHMARK_BINARY="./benchmarking_binary"

# default krympa binary
KRYMPA_BINARY_DEFAULT="./krympa"

# defaults
TIMEOUT_SECS=2700
KRYMPA_BINARY="$KRYMPA_BINARY_DEFAULT"
INPUT_PROVER="vampire"
SEEN_TIMEOUT=0
SEEN_BINARY=0
SEEN_PROVER=0

# argument parsing (order-independent):
# - one numeric timeout
# - one krympa binary path
# - optional prover: vampire|twee
for ARG in "$@"; do
    if [[ "$ARG" =~ ^[0-9]+$ ]]; then
        if [ $SEEN_TIMEOUT -eq 1 ]; then
            echo "Error: timeout specified more than once."
            exit 1
        fi
        TIMEOUT_SECS="$ARG"
        SEEN_TIMEOUT=1
    elif [ "$ARG" = "vampire" ] || [ "$ARG" = "twee" ]; then
        if [ $SEEN_PROVER -eq 1 ]; then
            echo "Error: prover specified more than once."
            exit 1
        fi
        INPUT_PROVER="$ARG"
        SEEN_PROVER=1
    else
        if [ $SEEN_BINARY -eq 1 ]; then
            echo "Error: krympa binary specified more than once."
            exit 1
        fi
        KRYMPA_BINARY="$ARG"
        SEEN_BINARY=1
    fi
done

if [ $# -gt 3 ]; then
    echo "Usage: $0 [timeout_secs] [krympa_binary] [vampire|twee]"
    echo "Arguments can be given in any order."
    exit 1
fi

mkdir -p "$BENCHMARK_DIR/output_logs"
# clean existing logs
rm -f "$BENCHMARK_DIR/output_logs"/*
ORIG_DIR=$(pwd)

echo "======================================"
echo "Krympa benchmark run starting"
echo "Timeout per benchmark : ${TIMEOUT_SECS}s"
echo "Krympa binary         : ${KRYMPA_BINARY}"
echo "Input prover          : ${INPUT_PROVER}"
echo "Logs directory        : ${BENCHMARK_DIR}/output_logs"
echo "======================================"
echo

run_one() {
    proofs_file="$1"
    base=$(basename "$proofs_file" .lean)

    echo "Processing $base ..."

    # Generate input using python script
    python3 "$PYTHON_SCRIPT" "$proofs_file"

    input_folder="$BENCHMARK_DIR/input${base//Proofs/}"
    if [ ! -d "$input_folder" ]; then
        echo "Input folder $input_folder not found! Skipping $base."
        return
    fi

    log_file="$BENCHMARK_DIR/output_logs/${base}.log"
    echo "Running benchmarking for $base (timeout=${TIMEOUT_SECS}s)..."

    cd "$RUST_DIR" || exit 1
    "$BENCHMARK_BINARY" "$input_folder" "$TIMEOUT_SECS" "$KRYMPA_BINARY" "$INPUT_PROVER" > "$log_file" 2>&1
    cd "$ORIG_DIR" || exit 1

    echo "Finished benchmarking for $base"
    echo "Log written to $log_file"
    echo
}

for proofs_file in "$BENCHMARK_DIR"/Proofs*.lean; do
    run_one "$proofs_file"
done

echo "All jobs completed."
echo "Detailed logs are available in: ${BENCHMARK_DIR}/output_logs"
